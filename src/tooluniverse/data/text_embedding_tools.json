[
    {
      "type": "TextEmbeddingTool",
      "name": "TextEmbedding",
      "description": "Generate text embeddings using Azure OpenAI embedding model. Supports single text or batch processing of multiple texts.",
      "parameter": {
        "type": "object",
        "properties": {
          "text": {
            "oneOf": [
              {
                "type": "string",
                "description": "Single text to generate embedding for"
              },
              {
                "type": "array",
                "description": "List of texts to generate embeddings for (batch processing)",
                "items": {
                  "type": "string"
                },
                "minItems": 1,
                "maxItems": 2048
              }
            ],
            "description": "Input text (string) or texts (array of strings) to generate embeddings for"
          }
        },
        "required": ["text"]
      },
      "model": "text-embedding-3-large",
      "required_api_keys": [
        "AZURE_OPENAI_API_KEY",
        "AZURE_OPENAI_ENDPOINT"
      ]
    }
  ]